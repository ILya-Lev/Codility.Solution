using MoreLinq;
using System;
using System.Collections.Generic;
using System.Linq;

namespace Codility.Solvers
{
    public class CanoeistDisplacement
    {
        //public List<Tuple<int, int>> FormTheDisplacement(int[] weights, int capacity)
        //{
        //    var sortedWeights = weights.OrderBy(w => w).ToArray();
        //    var first = 0;
        //    var last = weights.Length - 1;
        //    while (first < last)
        //    {
                
        //    }
        //}
    }
}
